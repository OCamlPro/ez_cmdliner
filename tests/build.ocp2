
build_tests = true;
try {
  build_tests = Features_get("tests", false).enabled;
} catch("unknown-variable",y){
  build_tests = true;
}

if( build_tests ){

OCaml.program("test1",
  ocaml + {
    files = [
      "test1.ml";
    ];
    requires = [
      "ezcmd";
    ];
  });

OCaml.program("test2",
  ocaml + {
    files = [
      "test2.ml";
    ];
    requires = [
      "ezcmd";
    ];
  });

OCaml.program("test3",
  ocaml + {
    files = [
      "test3.ml";
    ];
    requires = [
      "ezcmd";
    ];
  });

}
